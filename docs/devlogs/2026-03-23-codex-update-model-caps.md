# Summary
Updated model options, plan-based caps, and per-model overrides for context sizing in the overlay.

# Changes
- replaced model list with GPT-5.x/o3 options aligned to references and added plan selector
- added plan-gated model options and plan-based caps with per-model overrides
- updated model detection to recognize GPT-5.x, GPT-5 mini, and o3 labels

# Tests
Not run (not requested).

# Next
- Confirm UI behavior for plan gating and overrides in a live ChatGPT session.
