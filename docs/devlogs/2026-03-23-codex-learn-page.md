# Summary
Added a learn page that explains token/context estimation defaults and linked to it from the overlay panel.

# Changes
- added `learn.html` and `learn.css` with estimation summary, default caps table, and overhead assumptions
- added a "Learn how it works" link to the overlay panel that opens the learn page in a new tab
- styled the new footer link in the overlay

# Tests
Not run (not requested).

# Next
- Consider keeping the learn page in sync if model defaults change.
